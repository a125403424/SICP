If the interpreter is applicative-order, infinite recursion will happen.
Reason:
  The applicative-order interpreter will first evaluate the arguments then apply them,
  since one of the parameter of the new-if is the sqrt-iter procedure itself,
  the recursion will last untill stack overflow.

-------------------------------------------------------------------------------
Original answer (bad)
-------------------------------------------------------------------------------
The parameters for good-enough? are not enough, therefore there will be errors. 

Take (sqrt-iter 1 4) as example.

(new-if (good-enough? 1 4)
  1
  (sqrt-iter (improve 1 4) 4))

Check if the new-if works.

(new-if (good-enough? 1 4)
  (cond (good-enough? 1) -> not enough parameter
    (else 4))
